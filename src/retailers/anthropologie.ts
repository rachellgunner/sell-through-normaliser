import { notYetConfigured } from './types'

// TODO: replace with real column mapping once a sample Anthropologie
// export is available (not currently sent consistently). This was the
// last of the 8 retailers to be named — no sample data yet, so skuLevel
// is a guess (true, i.e. real product names expected) pending the real file.
export const anthropologie = notYetConfigured('anthropologie', 'Anthropologie', true)
